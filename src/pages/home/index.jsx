import React from 'react';
import { STYLES } from '../../constant/style';

const HomePage = () => {
  return (
    <div className={`${STYLES.background.bg_secondary} w-full h-max`}>
      <p>HomePage</p>
    </div>
  );
};

export default HomePage;
