import React from 'react';
import { STYLES } from '../../../constant/style';
const Header = () => {
  return (
    <div className={`${STYLES.background.bg_primary} h-20 shadow-md w-full`}>
      <p>Header</p>
    </div>
  );
};

export default Header;
